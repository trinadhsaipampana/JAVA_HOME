Install java by executing the following commands:
sudo apt update
sudo apt install openjdk-17-jdk -y
After installing java, we need to declare a global variable for it in /etc/profile file at the ending as:
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-arm64
After that we need to load it as global variable by executing the following command
source /etc/profile
